# Goal
앞서 작성한 `test_app.py`의 모든 테스트를 완벽하게 통과(GREEN 단계)할 수 있도록, 실제 작동하는 Flask 애플리케이션 코드를 구현합니다.

# Request
아래에 제공된 테스트 코드를 기반으로, 테스트를 하나도 빠짐없이 통과하는 `app.py` 코드를 작성하세요. 

test_app.py 을 참고하면 됩니다.

# Constraints
- Flask 프레임워크를 사용할 것.
- Python 3.10 이상을 사용하고 엄격한 Type Hints를 적용할 것.
- `app` 객체를 생성하고 `@app.route` 데코레이터를 사용하여 `/api/evaluate_risk` 엔드포인트를 구현할 것.
- 데이터 검증(음수 체크 등) 로직을 포함할 것.